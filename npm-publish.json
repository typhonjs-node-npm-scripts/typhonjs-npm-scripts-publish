/**
 * You can provide comments in `npm-publish.json`.
 *
 * The entry below is used in testing and is invoked in `./test/scripts/test.js`.
 */
{
   "prepublish":
   {
      "scripts":
      [
         "node ./test/scripts/fixture.js"
      ]
   }
}