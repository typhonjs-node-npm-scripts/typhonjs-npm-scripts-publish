/**
 * You can provide comments in `npm-pre-publish.json`.
 *
 * The entry below is used in testing and is invoked in `./test/scripts/test.js` via `npm run test`.
 */
{
   "scripts":
   [
      "node ./test/scripts/fixture.js"
   ]
}